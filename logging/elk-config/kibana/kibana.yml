# elk-config/kibana/kibana.yml

# Listen on all network interfaces so it can be accessed from your machine.
server.host: "0.0.0.0"

# The setting "elasticsearch.hosts" is now being set by the environment
# variable in the docker-compose.yml file, so it is not needed here.